{% extends '_layouts/in_app.html.twig' %}

{% block title %}Hello ListController!{% endblock %}

{% block content %}
    <h1>Liste des Portefeuilles</h1>
    <ul>
        {% for wallet in wallets %}
            <li>
                <a href="{{ path("wallets_show", {"uid": wallet.uid}) }}">{{ wallet.label }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
